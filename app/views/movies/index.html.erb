<%= render partial: 'shared/top_movies_button' %>
<%= render partial: 'shared/movie_search' %>
<%= render partial: 'shared/now_playing_button' %>

<% @movies.each do |movie| %>
  <div id='movie-<%= movie.id %>'><%= link_to movie.title, movie_path(movie.id) %>  Vote Average: <%= movie.vote_average %></div>
<% end %>
